let word = 'tryber';

let reverseWord = "";    //vai receber a nova palavra.

reverseWord = word.split('').reverse().join(''); // recebe palavra inversa

console.log(reverseWord);  //imprime a nova palavra 
